# 忠济堂中医养生连锁管理系统 · 编码规范（V3.3）

> **版本：V3.3 | 日期：2026年6月**
> **适用技术栈：Java(Spring Boot) / Vue3 / Python(FastAPI) / UniApp**

---

## 一、Java编码规范（Spring Boot后端）

### 1.1 项目结构

```
├── 包名规范: com.zhongjitang.{domain}.{module}
│   ├── controller       -- 控制层
│   ├── service         -- 服务层(接口)
│   ├── service.impl    -- 服务层(实现)
│   ├── repository      -- 数据访问层(MyBatis Mapper)
│   ├── entity          -- 实体类(DO)
│   ├── dto             -- 数据传输对象
│   │   ├── request     -- 请求DTO
│   │   └── response    -- 响应DTO
│   ├── vo              -- 视图对象
│   ├── enums           -- 枚举类
│   ├── config          -- 配置类
│   ├── exception       -- 异常定义
│   └── event           -- 领域事件
│
├── 类名规范:
│   ├── Controller:    {Entity}Controller, 例: MemberController
│   ├── Service:       I{Entity}Service, {Entity}ServiceImpl
│   ├── Mapper:        {Entity}Mapper
│   ├── Entity:        {Entity}DO
│   ├── DTO:           {Entity}Request/Response
│   ├── Enum:          {Entity}StatusEnum
│   ├── Config:        {Feature}Config
│   └── Exception:     {Feature}Exception
│
└── 方法命名:
    ├── 查询: get{Entity}ById / list{Entity}s / page{Entity}s / count{Entity}s
    ├── 新增: create{Entity} / save{Entity}
    ├── 更新: update{Entity} / modify{Entity}
    ├── 删除: delete{Entity}ById / remove{Entity}
    ├── 状态变更: {status}Entity, 例: confirmAppointment / cancelOrder
```

### 1.2 Controller规范

```java
@RestController
@RequestMapping("/api/v1/{domain}/{entity}")
@RequiredArgsConstructor
@Slf4j
@Validated
public class MemberController {

    private final IMemberService memberService;

    /**
     * 分页查询会员列表
     */
    @GetMapping
    public R<PageResult<MemberResponse>> listMembers(
            @RequestParam(required = false) Long storeId,
            @RequestParam(required = false) String keyword,
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "20") Integer pageSize) {

        MemberQuery query = MemberQuery.builder()
                .storeId(storeId)
                .keyword(keyword)
                .page(page)
                .pageSize(pageSize)
                .build();

        PageResult<MemberResponse> result = memberService.pageMembers(query);
        return R.ok(result);
    }

    /**
     * 获取会员详情
     */
    @GetMapping("/{id}")
    public R<MemberDetailResponse> getMember(@PathVariable Long id) {
        MemberDetailResponse detail = memberService.getMemberDetail(id);
        return R.ok(detail);
    }

    /**
     * 创建会员
     */
    @PostMapping
    public R<MemberResponse> createMember(@Valid @RequestBody MemberCreateRequest request) {
        MemberResponse member = memberService.createMember(request);
        return R.created(member);
    }

    /**
     * 更新会员体质
     */
    @PutMapping("/{id}/constitution")
    public R<Void> updateConstitution(
            @PathVariable Long id,
            @Valid @RequestBody ConstitutionUpdateRequest request) {
        memberService.updateConstitution(id, request);
        return R.ok();
    }
}
```

### 1.3 Service规范

```java
@Service
@RequiredArgsConstructor
@Slf4j
@Transactional(rollbackFor = Exception.class)
public class MemberServiceImpl implements IMemberService {

    private final MemberMapper memberMapper;
    private final MemberAccountMapper accountMapper;
    private final EventPublisher eventPublisher;

    @Override
    public MemberDetailResponse getMemberDetail(Long id) {
        Long tenantId = TenantContext.getTenantId(); // 多租户获取

        MemberDO member = memberMapper.selectByIdAndTenant(id, tenantId)
                .orElseThrow(() -> new MemberException(MemberErrorCode.NOT_FOUND));

        MemberDetailResponse response = convertToDetail(member);

        // 填充账户信息
        List<MemberAccountDO> accounts = accountMapper.selectByMemberId(id, tenantId);
        response.setAccounts(accounts);

        // 填充消费记录(最近10条)
        List<MemberConsumeRecord> records = memberMapper.selectRecentConsumes(id, tenantId, 10);
        response.setRecentConsumes(records);

        return response;
    }

    @Override
    @Transactional(isolation = Isolation.READ_COMMITTED)
    public MemberResponse createMember(MemberCreateRequest request) {
        Long tenantId = TenantContext.getTenantId();

        // 1. 校验手机号唯一
        if (memberMapper.existsByPhone(tenantId, request.getPhone())) {
            throw new MemberException(MemberErrorCode.PHONE_DUPLICATED);
        }

        // 2. 生成会员编号
        String memberNo = generateMemberNo(tenantId);

        // 3. 构建实体
        MemberDO member = MemberDO.builder()
                .tenantId(tenantId)
                .storeId(request.getStoreId())
                .memberNo(memberNo)
                .name(request.getName())
                .phone(request.getPhone())
                .gender(request.getGender())
                .birthday(request.getBirthday())
                .sourceChannel(request.getSourceChannel())
                .memberType(MemberType.PERSONAL.getCode())
                .status(MemberStatus.NORMAL.getCode())
                .points(0)
                .balance(BigDecimal.ZERO)
                .visitCount(0)
                .build();

        // 4. 保存
        memberMapper.insert(member);

        // 5. 创建初始账户
        createDefaultAccounts(member.getId(), tenantId);

        // 6. 发布领域事件
        eventPublisher.publish(MemberCreatedEvent.builder()
                .memberId(member.getId())
                .tenantId(tenantId)
                .sourceChannel(request.getSourceChannel())
                .build());

        return convertToResponse(member);
    }
}
```

### 1.4 多租户实现

```java
// TenantContext 租户上下文
public class TenantContext {
    private static final ThreadLocal<Long> TENANT_ID = new ThreadLocal<>();

    public static Long getTenantId() {
        Long id = TENANT_ID.get();
        Assert.notNull(id, "租户ID不能为空");
        return id;
    }

    public static void setTenantId(Long tenantId) {
        TENANT_ID.set(tenantId);
    }

    public static void clear() {
        TENANT_ID.remove();
    }
}

// TenantInterceptor 租户拦截器
@Component
@RequiredArgsConstructor
public class TenantInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        String token = request.getHeader("Authorization");
        Claims claims = JwtUtil.parseToken(token);
        Long tenantId = claims.get("tenant_id", Long.class);
        TenantContext.setTenantId(tenantId);
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) {
        TenantContext.clear();
    }
}

// MyBatis Plus 多租户插件配置
@Bean
public MybatisPlusInterceptor mybatisPlusInterceptor() {
    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
    interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(new TenantLineHandler() {
        @Override
        public Expression getTenantId() {
            return new LongValue(TenantContext.getTenantId());
        }

        @Override
        public String getTenantIdColumn() {
            return "tenant_id";
        }

        @Override
        public boolean ignoreTable(String tableName) {
            // 系统表忽略租户
            return Arrays.asList("sys_user", "sys_dict", "sys_config").contains(tableName);
        }
    }));
    return interceptor;
}
```

### 1.5 统一响应与异常

```java
// 统一响应
@Data
public class R<T> {
    private int code;
    private String message;
    private T data;
    private String requestId;
    private String timestamp;

    public static <T> R<T> ok(T data) {
        R<T> r = new R<>();
        r.setCode(0);
        r.setMessage("success");
        r.setData(data);
        r.setRequestId(RequestContext.getRequestId());
        r.setTimestamp(LocalDateTime.now().format(DateTimeFormatter.ISO_LOCAL_DATE_TIME));
        return r;
    }

    public static <T> R<T> created(T data) {
        R<T> r = ok(data);
        r.setCode(201);
        r.setMessage("created");
        return r;
    }

    public static <T> R<T> fail(int code, String message) {
        R<T> r = new R<>();
        r.setCode(code);
        r.setMessage(message);
        r.setRequestId(RequestContext.getRequestId());
        r.setTimestamp(LocalDateTime.now().format(DateTimeFormatter.ISO_LOCAL_DATE_TIME));
        return r;
    }
}

// 全局异常处理
@RestControllerAdvice
@RequiredArgsConstructor
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public R<Void> handleBusiness(BusinessException e) {
        log.warn("业务异常: code={}, message={}", e.getCode(), e.getMessage());
        return R.fail(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public R<Void> handleValidation(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
                .map(FieldError::getDefaultMessage)
                .findFirst()
                .orElse("参数校验失败");
        return R.fail(40001, message);
    }

    @ExceptionHandler(Exception.class)
    public R<Void> handleGeneral(Exception e) {
        log.error("系统异常", e);
        return R.fail(50001, "系统内部错误");
    }
}
```

---

## 二、Vue3前端编码规范

### 2.1 目录结构

```
src/
├── api/                 # API接口定义
│   ├── user/
│   │   ├── member.ts
│   │   └── employee.ts
│   └── trade/
│       ├── order.ts
│       └── appointment.ts
├── components/          # 组件
│   ├── common/         # 通用组件 (@/components/common/)
│   └── business/       # 业务组件
├── composables/         # 组合式函数
│   ├── useMember.ts
│   └── useAppointment.ts
├── pages/              # 页面
├── stores/             # Pinia状态管理
├── types/              # TypeScript类型
├── utils/              # 工具函数
├── styles/             # 全局样式
└── App.vue
```

### 2.2 API定义规范

```typescript
// api/user/member.ts
import { get, post, put } from '@/utils/request'
import type { Member, MemberQuery, MemberCreateRequest, PageResult } from '@/types/user'

/**
 * 分页查询会员列表
 */
export const listMembers = (params: MemberQuery) => {
  return get<PageResult<Member>>('/api/v1/user/members', params)
}

/**
 * 获取会员详情
 */
export const getMember = (id: number) => {
  return get<Member>(`/api/v1/user/members/${id}`)
}

/**
 * 创建会员
 */
export const createMember = (data: MemberCreateRequest) => {
  return post<Member>('/api/v1/user/members', data)
}

/**
 * 更新会员体质
 */
export const updateMemberConstitution = (id: number, data: ConstitutionUpdateRequest) => {
  return put(`/api/v1/user/members/${id}/constitution`, data)
}
```

### 2.3 组件规范

```vue
<template>
  <div class="member-list">
    <!-- 搜索区域 -->
    <div class="search-bar">
      <el-input v-model="query.keyword" placeholder="姓名/手机/会员号" clearable />
      <el-select v-model="query.status" placeholder="状态">
        <el-option label="全部" :value="undefined" />
        <el-option label="正常" :value="1" />
        <el-option label="冻结" :value="2" />
      </el-select>
      <el-button type="primary" @click="handleSearch">搜索</el-button>
      <el-button @click="handleReset">重置</el-button>
    </div>

    <!-- 数据表格 -->
    <el-table :data="list" v-loading="loading" stripe>
      <el-table-column prop="memberNo" label="会员编号" width="160" />
      <el-table-column prop="name" label="姓名" width="100" />
      <el-table-column prop="phone" label="手机号" width="130" />
      <el-table-column prop="memberLevel" label="等级" width="100">
        <template #default="{ row }">
          <el-tag :type="getLevelTagType(row.memberLevel)">
            {{ row.memberLevel }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="points" label="积分" width="100" align="right" />
      <el-table-column prop="balance" label="余额" width="100" align="right" />
      <el-table-column label="操作" width="150" fixed="right">
        <template #default="{ row }">
          <el-button link type="primary" @click="handleView(row)">详情</el-button>
          <el-button link type="primary" @click="handleEdit(row)">编辑</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="query.page"
      v-model:page-size="query.pageSize"
      :total="pagination.total"
      :page-sizes="[10, 20, 50]"
      layout="total, sizes, prev, pager, next"
      @change="fetchList"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { listMembers } from '@/api/user/member'
import type { Member, MemberQuery } from '@/types/user'

const router = useRouter()
const loading = ref(false)
const list = ref<Member[]>([])
const query = reactive<MemberQuery>({
  keyword: '',
  status: undefined,
  page: 1,
  pageSize: 20
})
const pagination = reactive({
  total: 0,
  page: 1,
  pageSize: 20
})

const fetchList = async () => {
  loading.value = true
  try {
    const res = await listMembers(query)
    list.value = res.data.list
    pagination.total = res.data.pagination.total
  } finally {
    loading.value = false
  }
}

const handleSearch = () => {
  query.page = 1
  fetchList()
}

const handleReset = () => {
  query.keyword = ''
  query.status = undefined
  query.page = 1
  fetchList()
}

const handleView = (row: Member) => {
  router.push(`/member/${row.id}`)
}

onMounted(fetchList)
</script>

<style scoped>
.member-list {
  padding: 16px;
}
.search-bar {
  display: flex;
  gap: 12px;
  margin-bottom: 16px;
}
</style>
```

---

## 三、Python AI服务编码规范（FastAPI）

### 3.1 项目结构

```
ai-service/
├── app/
│   ├── main.py              # FastAPI入口
│   ├── config.py            # 配置
│   ├── api/                 # API路由
│   │   ├── v1/
│   │   │   ├── chat.py
│   │   │   ├── tongue.py
│   │   │   └── report.py
│   │   └── deps.py          # 依赖注入
│   ├── core/                # 核心模块
│   │   ├── llm_gateway.py    # LLM网关
│   │   ├── router.py        # 模型路由
│   │   └── billing.py       # 计费计量
│   ├── services/            # 业务服务
│   │   ├── consultation.py
│   │   ├── tongue.py
│   │   └── report.py
│   ├── models/              # 数据模型
│   │   ├── request.py
│   │   └── response.py
│   └── utils/
└── tests/
```

### 3.2 FastAPI路由规范

```python
# app/api/v1/chat.py
from fastapi import APIRouter, Depends, HTTPException
from app.core.llm_gateway import LLMGateway
from app.models.request import ChatRequest
from app.models.response import ChatResponse
from app.api.deps import get_llm_gateway, get_current_tenant

router = APIRouter()


@router.post("/chat", response_model=ChatResponse)
async def chat(
    request: ChatRequest,
    tenant_id: str = Depends(get_current_tenant),
    llm_gateway: LLMGateway = Depends(get_llm_gateway),
) -> ChatResponse:
    """
    通用对话接口
    - task_type: chat_customer/chat_internal/content_generation
    """
    # 1. 限流检查
    await llm_gateway.check_rate_limit(tenant_id, request.task_type)

    # 2. 模型路由
    model = llm_gateway.route(request.task_type)

    # 3. 调用LLM
    result = await llm_gateway.chat(
        model=model,
        messages=request.messages,
        metadata={
            "tenant_id": tenant_id,
            "task_type": request.task_type,
        }
    )

    # 4. 计费记录
    await llm_gateway.record_usage(
        tenant_id=tenant_id,
        task_type=request.task_type,
        model=model,
        usage=result.usage,
        cost_cents=result.cost_cents,
    )

    return ChatResponse(
        request_id=result.request_id,
        model_used=model,
        latency_ms=result.latency_ms,
        usage=result.usage,
        choices=result.choices,
        cost_cents=result.cost_cents,
    )
```

---

## 四、Git提交规范

### 4.1 Commit Message格式

```
<type>(<scope>): <subject>

[可选 body]

[可选 footer]
```

**Type类型：**
| Type | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug修复 |
| docs | 文档变更 |
| style | 代码格式（不影响功能） |
| refactor | 重构（不是新功能也不是修复） |
| perf | 性能优化 |
| test | 测试相关 |
| chore | 构建/工具变更 |

**示例：**
```
feat(member): 新增会员体质辨识功能

- 接入AI网关体质辨识API
- 新增体质标签显示组件
- 体质报告支持分享

Closes #123
```

---

## 五、数据库规范

| 规范项 | 要求 |
|--------|------|
| 表名 | snake_case, {domain}_{entity}, 如 trade_order |
| 字段名 | snake_case, 如 created_at, member_id |
| 主键 | BIGINT自增或UUID |
| 多租户 | 所有业务表必须包含 tenant_id BIGINT NOT NULL |
| 索引 | idx_{table}_{columns}, 最多3列复合索引 |
| 金额 | DECIMAL(10,2)，禁止用DOUBLE |
| 时间 | DATETIME，精确到秒 |
| 软删除 | is_deleted TINYINT DEFAULT 0 |
| 审计字段 | created_at/updated_at/created_by/updated_by |
| 禁止 | 禁止用外键约束，应用层保证 |

---

## 六、日志规范

```java
// 使用SLF4J日志
@Slf4j
public class MemberServiceImpl {

    public void createMember(MemberCreateRequest request) {
        log.info("[创建会员] name={}, phone={}, sourceChannel={}",
                request.getName(), request.getPhone(), request.getSourceChannel());

        try {
            // 业务逻辑
            log.debug("[创建会员] 会员创建成功, id={}", member.getId());
        } catch (Exception e) {
            log.error("[创建会员] 会员创建失败, name={}, error={}",
                    request.getName(), e.getMessage(), e);
            throw e;
        }
    }
}
```

**日志级别使用：**
| 级别 | 使用场景 |
|------|----------|
| ERROR | 系统错误，需要人工介入 |
| WARN | 警告，可能有问题但不影响流程 |
| INFO | 关键业务节点，如创建订单、支付成功 |
| DEBUG | 开发调试信息，生产环境关闭 |

---

## 七、安全规范

| 规范 | 要求 |
|------|------|
| 参数校验 | 所有入参使用@Valid + JSR303注解校验 |
| SQL注入 | 使用MyBatis Parameter方式，禁止拼接SQL |
| XSS | 输入输出做HTML转义 |
| 敏感数据 | 手机号/身份证/银行卡号后端脱敏返回 |
| 密码 | BCrypt加密存储 |
| API限流 | 所有API接入限流，套餐决定配额 |
| 权限 | 基于RBAC，数据按租户+门店+个人过滤 |
| 日志脱敏 | 日志中脱敏手机号/身份证等信息 |

> **文档版本：V3.3**
> **编制日期：2026年6月**
> **审核状态：草稿**
