# 忠济堂中医养生连锁管理系统 · AI自动化开发手册（V3.3）

> **版本：V3.3 | 日期：2026年6月**
> **说明：指导开发团队如何使用AI工具（Copilot/Claude/GitHub Copilot等）提升开发效率，包含提示词模板、最佳实践和质量保障**

---

## 一、AI辅助开发工作流

### 1.1 核心原则

| 原则 | 说明 |
|------|------|
| **AI为辅，人为主** | AI生成代码需人工审核，不可直接上线 |
| **上下文传递** | 给AI足够的项目上下文再提问 |
| **增量迭代** | 一次一个小任务，逐步完善 |
| **安全红线** | 密钥/密码/敏感数据禁止传入AI |

### 1.2 开发阶段AI辅助矩阵

| 开发阶段 | AI辅助场景 | 效率提升 |
|----------|------------|----------|
| 需求分析 | 生成需求文档初稿、分解任务 | 30% |
| 架构设计 | 评审设计方案、补充遗漏点 | 20% |
| 代码编写 | 生成CRUD代码、工具类、单测 | 40% |
| 代码审查 | 发现潜在Bug、安全漏洞 | 25% |
| 文档编写 | 生成API文档、注释、README | 50% |
| 测试用例 | 生成单元测试、E2E测试用例 | 35% |
| Bug修复 | 分析根因、生成修复方案 | 30% |

---

## 二、后端开发AI提示词模板

### 2.1 生成CRUD服务

**提示词模板：**
```
你是一名Java后端工程师。请为以下需求生成完整的Service层代码：

上下文：
- 项目：忠济堂V3.3养生管理系统
- 框架：Spring Boot 2.7 + MyBatis Plus
- 规范：多租户(tenant_id)、统一响应R<T>、全局异常处理
- 代码路径：com.zhongjitang.{domain}.service

需求：
生成 [实体名]Service，包含：
1. 接口I{Entity}Service
2. 实现{Entity}ServiceImpl
3. Mapper接口{Entity}Mapper
4. Entity类{Entity}DO
5. Request/Response DTO

实体字段：
- id (Long, PK)
- tenant_id (Long, 多租户)
- name (String, 名称)
- phone (String, 手机号)
- status (Integer, 状态:1正常,2冻结)
- created_at (LocalDateTime)
- updated_at (LocalDateTime)
- is_deleted (Integer)

要求：
- 使用MyBatis Plus CRUD
- 包含分页查询方法
- 包含状态变更方法
- 包含租户隔离(tenant_id)
- 使用Lombok @Data
- 包含Swagger注解
```

**示例输出：**
```java
// I{Entity}Service.java
public interface IEntityService {
    PageResult<EntityResponse> pageEntities(EntityQuery query);
    EntityDetailResponse getEntityDetail(Long id);
    EntityResponse createEntity(EntityCreateRequest request);
    void updateEntity(Long id, EntityUpdateRequest request);
    void changeStatus(Long id, Integer status);
}

// {Entity}ServiceImpl.java
@Service
@RequiredArgsConstructor
@Slf4j
@Transactional(rollbackFor = Exception.class)
public class EntityServiceImpl implements IEntityService {

    private final EntityMapper entityMapper;

    @Override
    public PageResult<EntityResponse> pageEntities(EntityQuery query) {
        Long tenantId = TenantContext.getTenantId();
        Page<EntityDO> page = new Page<>(query.getPage(), query.getPageSize());
        LambdaQueryWrapper<EntityDO> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(EntityDO::getTenantId, tenantId);
        if (query.getStatus() != null) {
            wrapper.eq(EntityDO::getStatus, query.getStatus());
        }
        if (StringUtils.hasText(query.getKeyword())) {
            wrapper.and(w -> w.like(EntityDO::getName, query.getKeyword())
                    .or().like(EntityDO::getPhone, query.getKeyword()));
        }
        wrapper.orderByDesc(EntityDO::getCreatedAt);
        Page<EntityDO> result = entityMapper.selectPage(page, wrapper);
        return PageResult.of(result, EntityResponse.class);
    }
}
```

### 2.2 生成单元测试

**提示词模板：**
```
为以下Service方法生成完整的单元测试：

上下文：
- 测试框架：JUnit 5 + Mockito
- 断言库：AssertJ
- 包路径：src/test/java/com/zhongjitang/{domain}/service

待测试方法：
```java
public MemberDetailResponse getMemberDetail(Long id) {
    Long tenantId = TenantContext.getTenantId();
    MemberDO member = memberMapper.selectByIdAndTenant(id, tenantId)
            .orElseThrow(() -> new MemberException(MemberErrorCode.NOT_FOUND));
    // ...填充账户、消费记录等
    return response;
}
```

要求：
1. 使用@ExtendWith(MockitoExtension.class)
2. Mock依赖的Mapper
3. 模拟TenantContext.getTenantId()
4. 覆盖：正常查询、ID不存在抛出异常
5. 使用AssertJ断言
6. 包含中文注释
```

### 2.3 生成API端点

**提示词模板：**
```
为以下业务场景生成Spring Boot Controller代码：

上下文：
- 统一响应：R<T>
- 分页：PageResult<T>
- 校验：@Valid + @RequestBody
- 路径：/api/v1/{domain}/{entity}

业务场景：[描述业务场景]

要求：
1. RESTful风格
2. 包含Swagger @ApiOperation
3. 分页接口GET带分页参数
4. 详情接口GET /{id}
5. 创建接口POST
6. 更新接口PUT /{id}
7. 删除接口DELETE /{id}
8. 状态变更PUT /{id}/status
```

---

## 三、前端开发AI提示词模板

### 3.1 生成Vue3页面

**提示词模板：**
```
生成Vue3+TypeScript+Element Plus会员列表页面：

上下文：
- 技术栈：Vue3 Composition API + TypeScript + Element Plus
- 项目路径：src/pages/member/
- API调用：src/api/user/member.ts
- 类型定义：src/types/user.ts

功能需求：
1. 搜索栏：姓名/手机号关键字搜索、状态下拉、创建日期范围
2. 数据表格：会员编号、姓名、手机、等级、积分、余额、操作列
3. 分页：total/page/pageSize
4. 操作：详情按钮、编辑按钮
5. 加载态：v-loading

要求：
1. 使用 <script setup lang="ts">
2. 使用 reactive 管理查询参数
3. 使用 ref 管理列表数据和分页
4. 使用 async/await 调用API
5. 包含 loading 状态
6. 包含空数据提示
7. 表格使用 el-table-column
8. 搜索和重置按钮
```

### 3.2 生成Composable函数

**提示词模板：**
```
生成Vue3 Composable函数，管理会员相关的状态和逻辑：

上下文：
- 框架：Vue3 Composition API + TypeScript
- 路径：src/composables/useMember.ts

功能：
1. 状态：member (单个会员)、list (会员列表)、loading、pagination
2. 方法：fetchMember(id)、fetchList(query)、createMember(data)、updateMember(id, data)
3. 自动管理loading状态
4. 自动处理错误提示(ElMessage)

要求：
1. 使用泛型约束
2. 返回响应式状态和方法
3. 包含JSDoc注释
```

---

## 四、AI问诊服务开发指南

### 4.1 体质辨识Prompt模板

```python
CONSULTATION_PROMPT = """
你是忠济堂中医养生顾问。请根据客户的描述进行健康咨询。

客户信息：
- 姓名：{name}
- 性别：{gender}
- 年龄：{age}
- 已知体质：{constitution_type}（如有）
- 过敏史：{allergy_history}（如有）

主诉：
{chief_complaint}

已有症状：
{symptoms}

请按以下格式回复：

## 初步分析
[分析客户的健康状况]

## 体质关联
[分析与哪种体质最相关，1-9种体质分类]

## 推荐项目
请推荐2-3个适合的服务项目：
1. [项目名称] - [推荐理由]
2. [项目名称] - [推荐理由]
3. [项目名称] - [推荐理由]

## 调理建议
[3-5条调理建议]

## 注意事项
[特别提醒，如需线下就医的情况]

---
⚠️ 本分析仅供参考，不作为医疗诊断依据。如有严重不适，请及时就医。
"""
```

### 4.2 舌诊分析Prompt

```python
TONGUE_DIAGNOSIS_PROMPT = """
你是一位专业的中医舌诊AI。请分析以下舌象图片并给出判断。

分析维度：
1. 舌色（淡红舌/淡白舌/红舌/绛舌/青紫舌）
2. 舌苔（薄白苔/薄黄苔/黄苔/腻苔/厚苔/少苔）
3. 舌形（正常/胖大舌/齿痕舌/裂纹舌/瘦薄舌）
4. 舌下络脉（正常/迂曲/色暗）

请返回JSON格式：
{
  "tongue_color": "舌色描述",
  "tongue_coating": "舌苔描述",
  "tongue_shape": "舌形描述",
  "sublingual_vein": "舌下络脉描述",
  "constitution_type": 1-9的体质数字,
  "constitution_name": "体质名称",
  "confidence": 0.0-1.0的置信度,
  "analysis": "分析说明",
  "recommendations": [
    {"item": "推荐项目", "reason": "推荐理由"}
  ]
}
"""
```

### 4.3 AI日报生成Prompt

```python
DAILY_REPORT_PROMPT = """
你是忠济堂连锁门店的AI店长。请根据以下今日经营数据，生成一份AI经营日报。

门店：{store_name}
日期：{report_date}

核心指标：
- 今日营收：¥{revenue}（昨日：¥{yesterday_revenue}）
- 今日客流：{customer_count}人（昨日：{yesterday_customer_count}人）
- 客单价：¥{avg_per_customer}（昨日：¥{yesterday_avg_per_customer}）
- 预约完成率：{completion_rate}%
- 复购率：{return_visit_rate}%
- 技师平均评分：{avg_rating}
- 目标完成率：{target_completion}%

近7日营收趋势：{revenue_trend}

请生成以下内容(JSON格式)：
{
  "summary": "一句话总结今日表现",
  "highlights": [
    "亮点1：具体描述",
    "亮点2：具体描述"
  ],
  "concerns": [
    "关注点1：具体描述",
    "关注点2：具体描述"
  ],
  "suggestions": [
    "建议1：具体描述和可执行动作",
    "建议2：具体描述和可执行动作"
  ],
  "tomorrow_focus": "明日重点关注事项"
}
"""
```

---

## 五、AI话术生成开发

### 5.1 话术生成Prompt库

```python
# 咨询话术
CONSULTATION_SCRIPT_PROMPT = """
你是忠济堂资深顾问。请根据以下场景生成销售话术。

场景：{scene}
客户信息：
- 需求：{customer_need}
- 顾虑：{customer_concern}
- 体质：{constitution_type}

请生成：
1. 开场白（30秒）
2. 需求挖掘话术（3个问题）
3. 方案介绍话术
4. 处理{primary_concern}顾虑的话术
5. 邀约到店话术
6. 确认预约话术

要求：话术自然、有同理心、专业，融入中医养生知识。
"""

# 升单话术
UPSELL_SCRIPT_PROMPT = """
你是忠济堂技师。请根据以下场景生成升单话术。

当前服务：{current_service}
客户信息：
- 体质：{constitution_type}
- 消费记录：{consumption_history}
- 本次诉求：{current_need}

升单目标：{target_service}

请生成：
1. 升单时机判断（何时开口）
2. 自然过渡话术
3. 升单推荐话术（结合体质）
4. 价格说明话术
5. 客户拒绝处理（常见3种拒绝及回应）

要求：升单要自然、不强推、以客户需求为中心。
"""

# 回访话术
RETURN_VISIT_SCRIPT_PROMPT = """
你是忠济堂客服。请根据以下信息生成回访话术。

客户：{customer_name}
上次服务：{last_service}
距上次服务：{days_ago}天
上次体验评分：{rating}

请生成：
1. 关心问候话术
2. 服务效果跟进话术
3. 调理建议（结合上次体质）
4. 邀约下次到店话术
5. 结束语

要求：温暖有耐心，不强行推销。
"""
```

---

## 六、AI辅助代码审查清单

### 6.1 审查Prompt模板

```
请审查以下代码是否存在以下问题：

上下文：
- 技术栈：Spring Boot / Vue3 / Python FastAPI
- 规范：多租户隔离、安全编码规范

待审查代码：
```[粘贴代码]
```

请检查：
1. 多租户隔离：是否正确注入和校验tenant_id？
2. 参数校验：是否有@Valid注解？是否校验必填？
3. 空指针风险：是否做null检查？
4. SQL注入：是否使用参数化查询？
5. 敏感数据：是否明文处理手机号/身份证？
6. 异常处理：是否try-catch？是否正确抛出业务异常？
7. 日志规范：是否正确记录INFO/ERROR日志？
8. 事务管理：写操作是否有@Transactional？
9. 性能问题：是否有N+1查询？是否需要缓存？
10. 代码风格：命名规范、可读性、注释完整性

请逐条指出问题，并给出修复建议。
```

---

## 七、AI辅助API文档生成

### 7.1 OpenAPI/Swagger自动生成

```java
// 开启Swagger自动生成
@Configuration
@EnableOpenApi
public class SwaggerConfig {
    @Bean
    public OpenAPI zhongjitangOpenAPI() {
        return OpenAPI.builder()
                .info(new Info()
                        .title("忠济堂V3.3 API文档")
                        .version("V3.3.0")
                        .description("面向连锁养生集团的全栈数字化平台API"))
                .components(new Components()
                        .addSecuritySchemes("bearerAuth",
                                new SecurityScheme()
                                        .type(SecurityScheme.Type.HTTP)
                                        .scheme("bearer")
                                        .bearerFormat("JWT"))
                        .addSchemas("MemberResponse", MemberResponse.class)
                        .addSchemas("PageResult", PageResult.class)
                        .addSchemas("R", R.class))
                .addSecurityItem(new SecurityRequirement().addList("bearerAuth"));
    }
}
```

### 7.2 注释自动生成Doc

```java
/**
 * 创建会员
 *
 * @param request 会员创建请求
 *        - name: 姓名，必填，最大50字符
 *        - phone: 手机号，必填，11位手机号格式
 *        - gender: 性别，1男2女
 *        - storeId: 门店ID，必填
 *        - sourceChannel: 来源渠道，如miniprogram/douyin/meituan
 * @return 创建成功的会员信息
 * @throws MemberException(PHONE_DUPLICATED) 手机号已存在
 * @throws MemberException(STORE_NOT_FOUND) 门店不存在
 *
 * @apiNote 示例：
 * POST /api/v1/user/members
 * {
 *   "name": "张三",
 *   "phone": "13800138000",
 *   "gender": 1,
 *   "storeId": 101,
 *   "sourceChannel": "miniprogram"
 * }
 */
MemberResponse createMember(@Valid @RequestBody MemberCreateRequest request);
```

---

## 八、AI辅助SQL生成

### 8.1 复杂查询Prompt

```
请为以下业务场景生成SQL：

场景：查询门店月度经营汇总，包含：
1. 营收（支付成功的订单总额）
2. 客流（服务完成的客户数）
3. 客单价（营收/客流）
4. 目标完成率
5. 复购率（本月有新消费的会员数/上月到店会员数）
6. 技师排行TOP5（按业绩）

条件：
- 按store_id过滤
- 按自然月统计
- 需要JOIN订单表、预约表、技师表
- 金额用DECIMAL(10,2)

请生成MySQL 8.0兼容的SQL，使用窗口函数优化性能。
```

---

## 九、AI开发效率工具链

| 场景 | 工具 | 使用方式 |
|------|------|----------|
| 代码补全 | GitHub Copilot | IDE插件，实时补全 |
| 代码审查 | Claude | 粘贴代码+审查Prompt |
| 代码生成 | Claude | 详细描述+上下文+模板 |
| SQL生成 | Claude | 描述需求+表结构 |
| 文档生成 | Claude | 提供上下文+文档模板 |
| Bug分析 | Claude | 粘贴错误日志+堆栈 |
| 性能分析 | Claude | 粘贴JVM/慢查询日志 |
| 接口Mock | Apifox | OpenAPI导入自动Mock |
| 单元测试 | Copilot | 方法上"生成单元测试"注释 |

---

> **文档版本：V3.3**
> **编制日期：2026年6月**
> **审核状态：草稿**
